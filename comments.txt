Ссылка на GitHub: 
https://github.com/ibnkir/mle-project-sprint-1

Итерация проверки: #2

Пояснения:
- Внес все комментарии, в т.ч. вернулся к целевой переменной price.

- По поводу названий таблиц, в БД уже есть исходная таблица flats, поэтому, мне кажется, что для 
объединенного датасета название flats_churn (т.е. мешанина) вполне подходит, ну или можно было назвать
flats_dataset. А как принято в профессиональной среде?

- По поводу комментария "Что делать с записями, для которых flat_id имеет одинаковое значение?":
Мне кажется, это исключено, потому что flat_id - это id, т.е. ключевой столбец, в исходной таблице flats,
просто я его переименовал.
Кроме того, точно так же мы делали и в заданиях спринта. И это логично, т.е. мы удаляем одиаковые объекты,
но которые по какой-то ошибке помечены разным id.