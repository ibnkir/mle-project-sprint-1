Ссылка на GitHub: 
https://github.com/ibnkir/mle-project-sprint-1

Итерация проверки: #2

Пояснения:
- Внес все комментарии, в т.ч. вернулся к целевой переменной price.

- По поводу названий таблиц, в БД уже есть исходная таблица flats, поэтому, мне кажется, для 
объединенного датасета название flats_churn (т.е. мешанина) вполне подходит, ну или можно было назвать
flats_dataset. А как принято в профессиональной среде?

- По поводу комментария "Что делать с записями, для которых flat_id имеет одинаковое значение?",
по идее, это исключено, т.к. flat_id - это ключ в исходной таблице flats,
просто я его переименовал.

- Файл data.py в DVC pipeline:
я не внес latitude и longitude в датасет для обучения, т.к. они почти константные, а studio - полностью константный признак (см. мой ноутбук с EDA в файле notebooks/eda.ipynb) 
